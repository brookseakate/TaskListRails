  <%= content_tag(:span) { t.title }%>
  <br />
  <%= link_to "view", task_path(t), class: "small button" %>
  <%= link_to "edit", edit_task_path(t), class: "small button secondary" %>
  <%= link_to "delete", task_path(t), method: :delete, class: "small button alert", data: { confirm: "Are you sure you want to delete this task?" } %>
