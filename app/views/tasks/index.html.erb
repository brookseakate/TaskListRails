<main>
  <%= content_tag(:h1) { "Tasks To Do" } %>
  <ul>
    <% @tasks.each do |t| %>
    <li>
      <%= link_to("#{t[:title]}", task_path(t)) %>
    </li>
    <% end %>
  </ul>
  <%= link_to "Add a new task", tasks_new_path %>
</main>
